name: Deploy React Vite App

on:
  push:
    branches:
      - master  # یا master اگه از اون استفاده می‌کنی

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🔧 Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 📦 Install dependencies
        run: npm install

      - name: 🏗 Build project
        run: npm run build
